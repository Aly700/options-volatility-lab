[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "options-volatility-lab"
version = "0.1.0"
description = "Research framework for option pricing, implied volatility, and dynamic hedging."
requires-python = ">=3.10"
